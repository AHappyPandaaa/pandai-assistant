# Core
PyQt6>=6.6.0
anthropic>=0.25.0
faster-whisper>=1.0.0
sounddevice>=0.4.6
numpy>=1.24.0
scipy>=1.10.0
keyboard>=0.13.5

# CUDA support for faster-whisper (install manually if needed):
# pip install nvidia-cublas-cu12 nvidia-cudnn-cu12
# OR install via: pip install faster-whisper[cuda]
